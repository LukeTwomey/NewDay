/* Nice to keep these values all in one place and import them where needed so
   nothing is hard-coded in multiple places, and there is one source of truth
   which is easily updated, once. */

export const KEY_CODE = {
  ESC: 27,
};

export const FOCUSABLE_ELEMENTS =
  'button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])';
